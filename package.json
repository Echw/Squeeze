{
  "name": "squeeze-workspace",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "workspaces": ["web"],
  "scripts": {
    "build:wasm": "node scripts/build-wasm.mjs",
    "dev": "node scripts/dev.mjs",
    "dev:web": "npm --workspace web run dev",
    "build": "npm run build:wasm && npm --prefix web run build",
    "test": "npm --workspace web run test && cargo test --workspace",
    "check": "cargo fmt --all -- --check && cargo test --workspace && npm --workspace web run check",
    "check:web": "npm --workspace web run check"
  }
}
