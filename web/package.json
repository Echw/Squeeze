{
  "name": "squeeze-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "check": "npm run test && npm run build && node ../scripts/check-npm-licenses.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "@jsquash/webp": "^1.5.0",
    "fflate": "0.8.3"
  },
  "devDependencies": {
    "sass": "1.93.2",
    "typescript": "5.9.3",
    "vite": "7.3.6",
    "vitest": "4.1.11"
  }
}
