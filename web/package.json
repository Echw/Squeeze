{
  "name": "squeeze-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "check": "npm run test && npm run build && node ../scripts/check-npm-licenses.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "fflate": "0.8.2"
  },
  "devDependencies": {
    "sass": "1.93.2",
    "typescript": "5.9.3",
    "vite": "7.1.7",
    "vitest": "3.2.4"
  }
}

